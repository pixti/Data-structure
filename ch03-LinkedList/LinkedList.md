## 1. 연결리스트(LinkedList)

LinkedList는 사용자 정의 클래스인 Node로 List ADT를 구현한 자료구조.

ArrayList의 단점이였던 객체(Object)에 대한 삽입/삭제가 O(N)이라는 단점을 해결하기 위해 고안되었습니다.

 ArrayList와 마찬가지로 크기를 동적으로 조정할 수 있고, 빈번하게 삽입/삭제가 일어날 때 유용합니다.
